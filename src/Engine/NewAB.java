package Engine;

//black alpha beta
import static Engine.AI.NUMBER_OF_WHITE_PIECES;
import static Engine.Pieces.checkWhiteEnPassantRightsSlow;
import Util.ChessConstants;
import java.util.List;

class NewAB {

    private NewAB() {

    }

    //black moves
    static int max(Board board, int depth, int alpha, int beta) {
        return 0;
    }

    //whtie moves
    static int min(Board board, int depth, int alpha, int beta) {
       return 0;
    }
}
